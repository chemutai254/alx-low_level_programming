#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *@int: prints an absolute value
 * Return: an integer
 */

int _abs(int)
{
int a, num;
a = _abs(num);
return (a);
_putchar(a);
_putchar('\n');
}
