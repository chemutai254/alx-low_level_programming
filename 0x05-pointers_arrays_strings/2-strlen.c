#include "main.h"

/**
 * _strlen - function that returns a length of a string
 * @s: pointer
 * Return: length of a string
 */

int _strlen(char *s)
{
	int len;

	s = "Holberton!";
	len = _strlen(s);
	return (len);
}
